import Link from "next/link";
import { ArrowClockwise, House, WarningCircle } from "@/components/ui/icons";
import { Button } from "@/components/ui/button";

export function ErrorState({ title = "We could not load this page", description = "The saved survey data could not be retrieved. Your local draft is safe while we reconnect.", reset, details }: { title?: string; description?: string; reset?: () => void; details?: string }) {
  return <section className="flex min-h-[420px] items-center justify-center rounded-2xl border border-red-200 bg-[var(--surface)] px-6 py-12 dark:border-red-900/60" role="alert"><div className="max-w-lg text-center"><div className="mx-auto grid size-14 place-items-center rounded-2xl bg-red-50 text-[var(--danger)] dark:bg-red-950/50"><WarningCircle size={30} weight="duotone" /></div><p className="eyebrow mt-5 text-[var(--danger)]">Something went wrong</p><h1 className="mt-2 text-2xl font-semibold">{title}</h1><p className="mt-3 text-sm leading-6 text-[var(--ink-muted)]">{description}</p>{details ? <p className="mt-3 rounded-xl bg-[var(--surface-muted)] p-3 text-left text-xs text-[var(--ink-muted)]">Reference: {details}</p> : null}<div className="mt-6 flex flex-wrap justify-center gap-3">{reset ? <Button variant="secondary" onClick={reset}><ArrowClockwise size={17} />Try again</Button> : null}<Link href="/dashboard"><Button variant="primary"><House size={17} />Back to dashboard</Button></Link></div><p className="mt-5 text-xs text-[var(--ink-muted)]">If this keeps happening, check your connection and confirm the Supabase project is available.</p></div></section>;
}
